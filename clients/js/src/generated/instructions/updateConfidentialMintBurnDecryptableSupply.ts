/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    AccountRole,
    combineCodec,
    getStructDecoder,
    getStructEncoder,
    getU8Decoder,
    getU8Encoder,
    transformEncoder,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlyAccount,
    type ReadonlySignerAccount,
    type ReadonlyUint8Array,
    type TransactionSigner,
    type WritableAccount,
} from '@solana/kit';
import { TOKEN_2022_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
    getDecryptableBalanceDecoder,
    getDecryptableBalanceEncoder,
    type DecryptableBalance,
    type DecryptableBalanceArgs,
} from '../types';

export const UPDATE_CONFIDENTIAL_MINT_BURN_DECRYPTABLE_SUPPLY_DISCRIMINATOR = 42;

export function getUpdateConfidentialMintBurnDecryptableSupplyDiscriminatorBytes() {
    return getU8Encoder().encode(UPDATE_CONFIDENTIAL_MINT_BURN_DECRYPTABLE_SUPPLY_DISCRIMINATOR);
}

export const UPDATE_CONFIDENTIAL_MINT_BURN_DECRYPTABLE_SUPPLY_CONFIDENTIAL_MINT_BURN_DISCRIMINATOR = 2;

export function getUpdateConfidentialMintBurnDecryptableSupplyConfidentialMintBurnDiscriminatorBytes() {
    return getU8Encoder().encode(UPDATE_CONFIDENTIAL_MINT_BURN_DECRYPTABLE_SUPPLY_CONFIDENTIAL_MINT_BURN_DISCRIMINATOR);
}

export type UpdateConfidentialMintBurnDecryptableSupplyInstruction<
    TProgram extends string = typeof TOKEN_2022_PROGRAM_ADDRESS,
    TAccountMint extends string | AccountMeta<string> = string,
    TAccountAuthority extends string | AccountMeta<string> = string,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountMint extends string ? WritableAccount<TAccountMint> : TAccountMint,
            TAccountAuthority extends string ? ReadonlyAccount<TAccountAuthority> : TAccountAuthority,
            ...TRemainingAccounts,
        ]
    >;

export type UpdateConfidentialMintBurnDecryptableSupplyInstructionData = {
    discriminator: number;
    confidentialMintBurnDiscriminator: number;
    /** The new decryptable supply. */
    newDecryptableSupply: DecryptableBalance;
};

export type UpdateConfidentialMintBurnDecryptableSupplyInstructionDataArgs = {
    /** The new decryptable supply. */
    newDecryptableSupply: DecryptableBalanceArgs;
};

export function getUpdateConfidentialMintBurnDecryptableSupplyInstructionDataEncoder(): FixedSizeEncoder<UpdateConfidentialMintBurnDecryptableSupplyInstructionDataArgs> {
    return transformEncoder(
        getStructEncoder([
            ['discriminator', getU8Encoder()],
            ['confidentialMintBurnDiscriminator', getU8Encoder()],
            ['newDecryptableSupply', getDecryptableBalanceEncoder()],
        ]),
        value => ({
            ...value,
            discriminator: UPDATE_CONFIDENTIAL_MINT_BURN_DECRYPTABLE_SUPPLY_DISCRIMINATOR,
            confidentialMintBurnDiscriminator:
                UPDATE_CONFIDENTIAL_MINT_BURN_DECRYPTABLE_SUPPLY_CONFIDENTIAL_MINT_BURN_DISCRIMINATOR,
        }),
    );
}

export function getUpdateConfidentialMintBurnDecryptableSupplyInstructionDataDecoder(): FixedSizeDecoder<UpdateConfidentialMintBurnDecryptableSupplyInstructionData> {
    return getStructDecoder([
        ['discriminator', getU8Decoder()],
        ['confidentialMintBurnDiscriminator', getU8Decoder()],
        ['newDecryptableSupply', getDecryptableBalanceDecoder()],
    ]);
}

export function getUpdateConfidentialMintBurnDecryptableSupplyInstructionDataCodec(): FixedSizeCodec<
    UpdateConfidentialMintBurnDecryptableSupplyInstructionDataArgs,
    UpdateConfidentialMintBurnDecryptableSupplyInstructionData
> {
    return combineCodec(
        getUpdateConfidentialMintBurnDecryptableSupplyInstructionDataEncoder(),
        getUpdateConfidentialMintBurnDecryptableSupplyInstructionDataDecoder(),
    );
}

export type UpdateConfidentialMintBurnDecryptableSupplyInput<
    TAccountMint extends string = string,
    TAccountAuthority extends string = string,
> = {
    /** The SPL Token mint. */
    mint: Address<TAccountMint>;
    /** The confidential mint authority or its multisignature account. */
    authority: Address<TAccountAuthority> | TransactionSigner<TAccountAuthority>;
    newDecryptableSupply: UpdateConfidentialMintBurnDecryptableSupplyInstructionDataArgs['newDecryptableSupply'];
    multiSigners?: Array<TransactionSigner>;
};

export function getUpdateConfidentialMintBurnDecryptableSupplyInstruction<
    TAccountMint extends string,
    TAccountAuthority extends string,
    TProgramAddress extends Address = typeof TOKEN_2022_PROGRAM_ADDRESS,
>(
    input: UpdateConfidentialMintBurnDecryptableSupplyInput<TAccountMint, TAccountAuthority>,
    config?: { programAddress?: TProgramAddress },
): UpdateConfidentialMintBurnDecryptableSupplyInstruction<
    TProgramAddress,
    TAccountMint,
    (typeof input)['authority'] extends TransactionSigner<TAccountAuthority>
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority
> {
    // Program address.
    const programAddress = config?.programAddress ?? TOKEN_2022_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        mint: { value: input.mint ?? null, isWritable: true },
        authority: { value: input.authority ?? null, isWritable: false },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

    // Original args.
    const args = { ...input };

    // Remaining accounts.
    const remainingAccounts: AccountMeta[] = (args.multiSigners ?? []).map(signer => ({
        address: signer.address,
        role: AccountRole.READONLY_SIGNER,
        signer,
    }));

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [getAccountMeta(accounts.mint), getAccountMeta(accounts.authority), ...remainingAccounts],
        data: getUpdateConfidentialMintBurnDecryptableSupplyInstructionDataEncoder().encode(
            args as UpdateConfidentialMintBurnDecryptableSupplyInstructionDataArgs,
        ),
        programAddress,
    } as UpdateConfidentialMintBurnDecryptableSupplyInstruction<
        TProgramAddress,
        TAccountMint,
        (typeof input)['authority'] extends TransactionSigner<TAccountAuthority>
            ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
            : TAccountAuthority
    >);
}

export type ParsedUpdateConfidentialMintBurnDecryptableSupplyInstruction<
    TProgram extends string = typeof TOKEN_2022_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        /** The SPL Token mint. */
        mint: TAccountMetas[0];
        /** The confidential mint authority or its multisignature account. */
        authority: TAccountMetas[1];
    };
    data: UpdateConfidentialMintBurnDecryptableSupplyInstructionData;
};

export function parseUpdateConfidentialMintBurnDecryptableSupplyInstruction<
    TProgram extends string,
    TAccountMetas extends readonly AccountMeta[],
>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateConfidentialMintBurnDecryptableSupplyInstruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 2) {
        // TODO: Coded error.
        throw new Error('Not enough accounts');
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: { mint: getNextAccount(), authority: getNextAccount() },
        data: getUpdateConfidentialMintBurnDecryptableSupplyInstructionDataDecoder().decode(instruction.data),
    };
}
