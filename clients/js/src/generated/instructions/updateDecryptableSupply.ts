/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  AccountRole,
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { TOKEN_2022_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getDecryptableSupplyDecoder,
  getDecryptableSupplyEncoder,
  type DecryptableSupply,
  type DecryptableSupplyArgs,
} from '../types';

export const UPDATE_DECRYPTABLE_SUPPLY_DISCRIMINATOR = 42;

export function getUpdateDecryptableSupplyDiscriminatorBytes() {
  return getU8Encoder().encode(UPDATE_DECRYPTABLE_SUPPLY_DISCRIMINATOR);
}

export const UPDATE_DECRYPTABLE_SUPPLY_CONFIDENTIAL_MINT_BURN_DISCRIMINATOR = 2;

export function getUpdateDecryptableSupplyConfidentialMintBurnDiscriminatorBytes() {
  return getU8Encoder().encode(
    UPDATE_DECRYPTABLE_SUPPLY_CONFIDENTIAL_MINT_BURN_DISCRIMINATOR
  );
}

export type UpdateDecryptableSupplyInstruction<
  TProgram extends string = typeof TOKEN_2022_PROGRAM_ADDRESS,
  TAccountMint extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateDecryptableSupplyInstructionData = {
  discriminator: number;
  confidentialMintBurnDiscriminator: number;
  /** The new decryptable supply encrypted with the supply aes key */
  newDecryptableSupply: DecryptableSupply;
};

export type UpdateDecryptableSupplyInstructionDataArgs = {
  /** The new decryptable supply encrypted with the supply aes key */
  newDecryptableSupply: DecryptableSupplyArgs;
};

export function getUpdateDecryptableSupplyInstructionDataEncoder(): FixedSizeEncoder<UpdateDecryptableSupplyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['confidentialMintBurnDiscriminator', getU8Encoder()],
      ['newDecryptableSupply', getDecryptableSupplyEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_DECRYPTABLE_SUPPLY_DISCRIMINATOR,
      confidentialMintBurnDiscriminator:
        UPDATE_DECRYPTABLE_SUPPLY_CONFIDENTIAL_MINT_BURN_DISCRIMINATOR,
    })
  );
}

export function getUpdateDecryptableSupplyInstructionDataDecoder(): FixedSizeDecoder<UpdateDecryptableSupplyInstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['confidentialMintBurnDiscriminator', getU8Decoder()],
    ['newDecryptableSupply', getDecryptableSupplyDecoder()],
  ]);
}

export function getUpdateDecryptableSupplyInstructionDataCodec(): FixedSizeCodec<
  UpdateDecryptableSupplyInstructionDataArgs,
  UpdateDecryptableSupplyInstructionData
> {
  return combineCodec(
    getUpdateDecryptableSupplyInstructionDataEncoder(),
    getUpdateDecryptableSupplyInstructionDataDecoder()
  );
}

export type UpdateDecryptableSupplyInput<TAccountMint extends string = string> =
  {
    /** The SPL Token mint. */
    mint: Address<TAccountMint>;
    newDecryptableSupply: UpdateDecryptableSupplyInstructionDataArgs['newDecryptableSupply'];
    multiSigners?: Array<TransactionSigner>;
  };

export function getUpdateDecryptableSupplyInstruction<
  TAccountMint extends string,
  TProgramAddress extends Address = typeof TOKEN_2022_PROGRAM_ADDRESS,
>(
  input: UpdateDecryptableSupplyInput<TAccountMint>,
  config?: { programAddress?: TProgramAddress }
): UpdateDecryptableSupplyInstruction<TProgramAddress, TAccountMint> {
  // Program address.
  const programAddress = config?.programAddress ?? TOKEN_2022_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    mint: { value: input.mint ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Remaining accounts.
  const remainingAccounts: AccountMeta[] = (args.multiSigners ?? []).map(
    (signer) => ({
      address: signer.address,
      role: AccountRole.READONLY_SIGNER,
      signer,
    })
  );

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.mint), ...remainingAccounts],
    data: getUpdateDecryptableSupplyInstructionDataEncoder().encode(
      args as UpdateDecryptableSupplyInstructionDataArgs
    ),
    programAddress,
  } as UpdateDecryptableSupplyInstruction<TProgramAddress, TAccountMint>);
}

export type ParsedUpdateDecryptableSupplyInstruction<
  TProgram extends string = typeof TOKEN_2022_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The SPL Token mint. */
    mint: TAccountMetas[0];
  };
  data: UpdateDecryptableSupplyInstructionData;
};

export function parseUpdateDecryptableSupplyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateDecryptableSupplyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { mint: getNextAccount() },
    data: getUpdateDecryptableSupplyInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
