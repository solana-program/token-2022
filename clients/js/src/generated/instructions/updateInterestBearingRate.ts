/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  AccountRole,
  combineCodec,
  getI16Decoder,
  getI16Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/web3.js';
import { TOKEN_2022_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_INTEREST_BEARING_RATE_DISCRIMINATOR = 33;

export function getUpdateInterestBearingRateDiscriminatorBytes() {
  return getU8Encoder().encode(UPDATE_INTEREST_BEARING_RATE_DISCRIMINATOR);
}

export const UPDATE_INTEREST_BEARING_RATE_INTEREST_BEARING_DISCRIMINATOR = 1;

export function getUpdateInterestBearingRateInterestBearingDiscriminatorBytes() {
  return getU8Encoder().encode(
    UPDATE_INTEREST_BEARING_RATE_INTEREST_BEARING_DISCRIMINATOR
  );
}

export type UpdateInterestBearingRateInstruction<
  TProgram extends string = typeof TOKEN_2022_PROGRAM_ADDRESS,
  TAccountMint extends string | IAccountMeta<string> = string,
  TAccountRateAuthority extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountRateAuthority extends string
        ? ReadonlyAccount<TAccountRateAuthority>
        : TAccountRateAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateInterestBearingRateInstructionData = {
  discriminator: number;
  interestBearingDiscriminator: number;
  /** The new interest rate in basis points */
  rate: number;
};

export type UpdateInterestBearingRateInstructionDataArgs = {
  /** The new interest rate in basis points */
  rate: number;
};

export function getUpdateInterestBearingRateInstructionDataEncoder(): Encoder<UpdateInterestBearingRateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['interestBearingDiscriminator', getU8Encoder()],
      ['rate', getI16Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_INTEREST_BEARING_RATE_DISCRIMINATOR,
      interestBearingDiscriminator:
        UPDATE_INTEREST_BEARING_RATE_INTEREST_BEARING_DISCRIMINATOR,
    })
  );
}

export function getUpdateInterestBearingRateInstructionDataDecoder(): Decoder<UpdateInterestBearingRateInstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['interestBearingDiscriminator', getU8Decoder()],
    ['rate', getI16Decoder()],
  ]);
}

export function getUpdateInterestBearingRateInstructionDataCodec(): Codec<
  UpdateInterestBearingRateInstructionDataArgs,
  UpdateInterestBearingRateInstructionData
> {
  return combineCodec(
    getUpdateInterestBearingRateInstructionDataEncoder(),
    getUpdateInterestBearingRateInstructionDataDecoder()
  );
}

export type UpdateInterestBearingRateInput<
  TAccountMint extends string = string,
  TAccountRateAuthority extends string = string,
> = {
  /** The mint. */
  mint: Address<TAccountMint>;
  /** The mint rate authority. */
  rateAuthority:
    | Address<TAccountRateAuthority>
    | TransactionSigner<TAccountRateAuthority>;
  rate: UpdateInterestBearingRateInstructionDataArgs['rate'];
  multiSigners?: Array<TransactionSigner>;
};

export function getUpdateInterestBearingRateInstruction<
  TAccountMint extends string,
  TAccountRateAuthority extends string,
  TProgramAddress extends Address = typeof TOKEN_2022_PROGRAM_ADDRESS,
>(
  input: UpdateInterestBearingRateInput<TAccountMint, TAccountRateAuthority>,
  config?: { programAddress?: TProgramAddress }
): UpdateInterestBearingRateInstruction<
  TProgramAddress,
  TAccountMint,
  (typeof input)['rateAuthority'] extends TransactionSigner<TAccountRateAuthority>
    ? ReadonlySignerAccount<TAccountRateAuthority> &
        IAccountSignerMeta<TAccountRateAuthority>
    : TAccountRateAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? TOKEN_2022_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    mint: { value: input.mint ?? null, isWritable: true },
    rateAuthority: { value: input.rateAuthority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Remaining accounts.
  const remainingAccounts: IAccountMeta[] = (args.multiSigners ?? []).map(
    (signer) => ({
      address: signer.address,
      role: AccountRole.READONLY_SIGNER,
      signer,
    })
  );

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.rateAuthority),
      ...remainingAccounts,
    ],
    programAddress,
    data: getUpdateInterestBearingRateInstructionDataEncoder().encode(
      args as UpdateInterestBearingRateInstructionDataArgs
    ),
  } as UpdateInterestBearingRateInstruction<
    TProgramAddress,
    TAccountMint,
    (typeof input)['rateAuthority'] extends TransactionSigner<TAccountRateAuthority>
      ? ReadonlySignerAccount<TAccountRateAuthority> &
          IAccountSignerMeta<TAccountRateAuthority>
      : TAccountRateAuthority
  >;

  return instruction;
}

export type ParsedUpdateInterestBearingRateInstruction<
  TProgram extends string = typeof TOKEN_2022_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The mint. */
    mint: TAccountMetas[0];
    /** The mint rate authority. */
    rateAuthority: TAccountMetas[1];
  };
  data: UpdateInterestBearingRateInstructionData;
};

export function parseUpdateInterestBearingRateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedUpdateInterestBearingRateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      mint: getNextAccount(),
      rateAuthority: getNextAccount(),
    },
    data: getUpdateInterestBearingRateInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
