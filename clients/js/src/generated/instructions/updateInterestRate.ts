/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  AccountRole,
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/web3.js';
import { TOKEN_2022_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_INTEREST_RATE_DISCRIMINATOR = 37;

export function getUpdateInterestRateDiscriminatorBytes() {
  return getU8Encoder().encode(UPDATE_INTEREST_RATE_DISCRIMINATOR);
}

export const UPDATE_INTEREST_RATE_INTEREST_BEARING_DISCRIMINATOR = 1;

export function getUpdateInterestRateInterestBearingDiscriminatorBytes() {
  return getU8Encoder().encode(
    UPDATE_INTEREST_RATE_INTEREST_BEARING_DISCRIMINATOR
  );
}

export type UpdateInterestRateInstruction<
  TProgram extends string = typeof TOKEN_2022_PROGRAM_ADDRESS,
  TAccountMint extends string | IAccountMeta<string> = string,
  TAccountAuthority extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountAuthority extends string
        ? ReadonlyAccount<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateInterestRateInstructionData = {
  discriminator: number;
  interestBearingDiscriminator: number;
  /** The basis points for interest calculation */
  interestRateBasisPoints: number;
};

export type UpdateInterestRateInstructionDataArgs = {
  /** The basis points for interest calculation */
  interestRateBasisPoints: number;
};

export function getUpdateInterestRateInstructionDataEncoder(): Encoder<UpdateInterestRateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['interestBearingDiscriminator', getU8Encoder()],
      ['interestRateBasisPoints', getU16Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_INTEREST_RATE_DISCRIMINATOR,
      interestBearingDiscriminator:
        UPDATE_INTEREST_RATE_INTEREST_BEARING_DISCRIMINATOR,
    })
  );
}

export function getUpdateInterestRateInstructionDataDecoder(): Decoder<UpdateInterestRateInstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['interestBearingDiscriminator', getU8Decoder()],
    ['interestRateBasisPoints', getU16Decoder()],
  ]);
}

export function getUpdateInterestRateInstructionDataCodec(): Codec<
  UpdateInterestRateInstructionDataArgs,
  UpdateInterestRateInstructionData
> {
  return combineCodec(
    getUpdateInterestRateInstructionDataEncoder(),
    getUpdateInterestRateInstructionDataDecoder()
  );
}

export type UpdateInterestRateInput<
  TAccountMint extends string = string,
  TAccountAuthority extends string = string,
> = {
  /** The mint. */
  mint: Address<TAccountMint>;
  /** The mint rate authority. */
  authority: Address<TAccountAuthority> | TransactionSigner<TAccountAuthority>;
  interestRateBasisPoints: UpdateInterestRateInstructionDataArgs['interestRateBasisPoints'];
  multiSigners?: Array<TransactionSigner>;
};

export function getUpdateInterestRateInstruction<
  TAccountMint extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof TOKEN_2022_PROGRAM_ADDRESS,
>(
  input: UpdateInterestRateInput<TAccountMint, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): UpdateInterestRateInstruction<
  TProgramAddress,
  TAccountMint,
  (typeof input)['authority'] extends TransactionSigner<TAccountAuthority>
    ? ReadonlySignerAccount<TAccountAuthority> &
        IAccountSignerMeta<TAccountAuthority>
    : TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? TOKEN_2022_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    mint: { value: input.mint ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Remaining accounts.
  const remainingAccounts: IAccountMeta[] = (args.multiSigners ?? []).map(
    (signer) => ({
      address: signer.address,
      role: AccountRole.READONLY_SIGNER,
      signer,
    })
  );

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.authority),
      ...remainingAccounts,
    ],
    programAddress,
    data: getUpdateInterestRateInstructionDataEncoder().encode(
      args as UpdateInterestRateInstructionDataArgs
    ),
  } as UpdateInterestRateInstruction<
    TProgramAddress,
    TAccountMint,
    (typeof input)['authority'] extends TransactionSigner<TAccountAuthority>
      ? ReadonlySignerAccount<TAccountAuthority> &
          IAccountSignerMeta<TAccountAuthority>
      : TAccountAuthority
  >;

  return instruction;
}

export type ParsedUpdateInterestRateInstruction<
  TProgram extends string = typeof TOKEN_2022_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The mint. */
    mint: TAccountMetas[0];
    /** The mint rate authority. */
    authority: TAccountMetas[1];
  };
  data: UpdateInterestRateInstructionData;
};

export function parseUpdateInterestRateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedUpdateInterestRateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      mint: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getUpdateInterestRateInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
